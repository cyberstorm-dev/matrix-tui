[project]
name = "matrix-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "matrix-nio>=0.24",
    "aiohttp>=3.9",
    "aiofiles>=24.0",
    "litellm>=1.0",
    "pydantic-settings>=2.0",
    "redis>=5.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24", "ruff>=0.9", "fakeredis>=2.23"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/matrix_agent"]
